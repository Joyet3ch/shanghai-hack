This is a significant upgrade. Three new capabilities: **country scoring**, **country-level deep analysis**, and **live web search**. Let me design the complete implementation.

---

## The New Architecture

```
OLD FLOW:
Form → AI generates from training data → Report

NEW FLOW:
Form → Web search fetches live market data
     → AI reasons over real data
     → Country scorer ranks all countries
     → Deep analysis for best country
     → Report with live intelligence
```

---

## Step 1 — Add Web Search With Tavily

Tavily is a search API built specifically for AI agents. Free tier works perfectly for a hackathon.

**Sign up at tavily.com and get your API key (takes 2 minutes)**

Add to your environment variables:
```
TAVILY_API_KEY=tvly-your-key-here
```

Create `api/search.js` — your web search utility:

```javascript
// Reusable web search function for all API routes
export const searchWeb = async (query, maxResults = 5) => {
  const response = await fetch('https://api.tavily.com/search', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      api_key: process.env.TAVILY_API_KEY,
      query,
      search_depth: 'advanced',
      max_results: maxResults,
      include_answer: true,
      include_raw_content: false
    })
  });
  const data = await response.json();
  return data.results?.map(r => ({
    title: r.title,
    content: r.content,
    url: r.url
  })) || [];
};
```

---

## Step 2 — New Form Field

Add a country selector to your form in `App.jsx`:

```javascript
// Add to formData state
const [formData, setFormData] = useState({
  ...existing fields,
  target_continent: 'Europe',   // Europe | North America | Both
  target_country: 'recommend'   // 'recommend' or specific country name
});

// Countries by continent
const COUNTRIES = {
  Europe: [
    'Let AI Recommend',
    'Germany', 'France', 'Netherlands',
    'Italy', 'United Kingdom', 'Sweden',
    'Norway', 'Spain', 'Belgium', 'Poland'
  ],
  'North America': [
    'Let AI Recommend',
    'United States', 'Canada'
  ],
  Both: ['Let AI Recommend']
};
```

Add to your sidebar form:

```jsx
{/* Continent selector */}
<select
  value={formData.target_continent}
  onChange={e => setFormData(prev => ({
    ...prev,
    target_continent: e.target.value,
    target_country: 'recommend'
  }))}
  style={inputStyle}
>
  <option value="Europe">Europe</option>
  <option value="North America">North America</option>
  <option value="Both">Europe + North America</option>
</select>

{/* Country selector — only shown if continent selected */}
{formData.target_continent !== 'Both' && (
  <select
    value={formData.target_country}
    onChange={e => setFormData(prev => ({
      ...prev,
      target_country: e.target.value
    }))}
    style={inputStyle}
  >
    {COUNTRIES[formData.target_continent].map(country => (
      <option
        key={country}
        value={country === 'Let AI Recommend' ? 'recommend' : country}
      >
        {country}
      </option>
    ))}
  </select>
)}
```

---

## Step 3 — New `api/match.js` With Web Search + Country Intelligence

Replace your entire `match.js`:

```javascript
export const config = { runtime: 'edge' };

// ── Web search utility ────────────────────────────────────
const searchWeb = async (query, maxResults = 4) => {
  try {
    const response = await fetch('https://api.tavily.com/search', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        api_key: process.env.TAVILY_API_KEY,
        query,
        search_depth: 'advanced',
        max_results: maxResults,
        include_answer: true
      })
    });
    const data = await response.json();
    return {
      answer: data.answer || '',
      results: data.results?.map(r => ({
        title: r.title,
        content: r.content.slice(0, 400), // cap length
        url: r.url
      })) || []
    };
  } catch (e) {
    return { answer: '', results: [] };
  }
};

// ── Format search results for prompt injection ────────────
const formatSearch = (label, data) =>
  `[${label}]\nSummary: ${data.answer}\n` +
  data.results.map(r => `• ${r.title}: ${r.content}`).join('\n');

export default async function handler(req) {
  if (req.method !== 'POST') {
    return new Response(JSON.stringify({ error: 'Method Not Allowed' }),
      { status: 405, headers: { 'Content-Type': 'application/json' } });
  }

  const {
    company_name, product_description, business_model,
    target_continent, target_country,
    sector, company_stage, biggest_concern
  } = await req.json();

  if (!company_name || !product_description || !sector) {
    return new Response(JSON.stringify({ error: 'Missing required fields' }),
      { status: 400, headers: { 'Content-Type': 'application/json' } });
  }

  const isRecommendMode = !target_country || target_country === 'recommend';
  const continent = target_continent || 'Europe';

  // ── Run web searches in parallel ─────────────────────────
  // All searches run simultaneously to save time
  const [
    marketData,
    competitorData,
    regulatoryData,
    countryData,
    buyerData
  ] = await Promise.all([

    // 1. Market size and trends
    searchWeb(`${sector} market size growth ${continent} 2025 2026`),

    // 2. Western competitors
    searchWeb(`top ${sector} companies ${continent} suppliers market leaders 2025`),

    // 3. Regulations
    searchWeb(`${sector} regulations certification requirements ${continent} 2025 2026`),

    // 4. Country comparison (if recommending)
    isRecommendMode
      ? searchWeb(`best country ${continent} ${sector} market entry Chinese companies 2025`)
      : searchWeb(`${sector} market ${target_country} opportunities growth 2025 2026`),

    // 5. Buyer behavior
    searchWeb(`${sector} procurement buyers ${continent} supplier requirements 2025`)
  ]);

  // ── Build country context ─────────────────────────────────
  const countryContext = isRecommendMode
    ? `
TASK: Recommend the BEST SINGLE COUNTRY in ${continent} for this startup to enter first.
Analyze and score each country. Consider:
- Market size and growth rate
- Competition level (lower = easier entry)  
- Regulatory complexity (lower = easier entry)
- Cultural receptiveness to Chinese products
- Government incentives for this sector
- Distribution infrastructure quality
- Language accessibility for a Chinese company

Countries to evaluate for ${continent}:
${continent === 'Europe'
  ? 'Germany, France, Netherlands, Italy, UK, Sweden, Norway, Spain, Belgium, Poland'
  : 'United States, Canada'}
    `
    : `
TASK: Provide deep market analysis for ${target_country} specifically.
The user has already chosen ${target_country} as their target country.
All ICP matches and competitors must be based in ${target_country}.
    `;

  // ── System prompt ─────────────────────────────────────────
  const SYSTEM_PROMPT = `
You are Dr. Sarah Chen — a Principal at a top-tier strategy consultancy
with 12 years advising Chinese companies on Western market entry.
You have led 40+ market entry projects across Europe and North America.

You have access to LIVE MARKET RESEARCH gathered seconds ago.
Use this real data as the primary source for your analysis.
Do not rely on general knowledge when specific data is provided.

YOUR ANALYSIS PROCESS:
1. Read all live market research carefully
2. Identify the most relevant signals for this specific company
3. ${isRecommendMode
  ? 'Score each country systematically before recommending one'
  : `Deep-dive into ${target_country} market specifically`}
4. Match ICP companies based on actual market signals, not generic knowledge
5. Generate actionable, evidence-based output

CRITICAL RULES:
- Return ONLY valid JSON. No markdown, no preamble.
- Every insight must reference specific data from the live research provided.
- If the research confirms something, cite it. If it contradicts your training
  data, trust the live research.
- All companies in icp_matches and competitors must be REAL companies
  in ${isRecommendMode ? continent : target_country}.
- All scores are numbers, not strings.

LIVE MARKET RESEARCH:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

${formatSearch('MARKET SIZE & TRENDS', marketData)}

${formatSearch('COMPETITOR LANDSCAPE', competitorData)}

${formatSearch('REGULATORY ENVIRONMENT', regulatoryData)}

${formatSearch('COUNTRY INTELLIGENCE', countryData)}

${formatSearch('BUYER BEHAVIOR & PROCUREMENT', buyerData)}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
`;

  // ── User prompt with country logic ────────────────────────
  const USER_PROMPT = `
COMPANY PROFILE:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Name:            ${company_name}
Product:         ${product_description}
Business Model:  ${business_model}
Sector:          ${sector}
Stage:           ${company_stage}
Target Region:   ${continent}
Target Country:  ${isRecommendMode ? 'AI to recommend best country' : target_country}
Biggest Concern: ${biggest_concern}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

${countryContext}

Generate the complete market entry intelligence report.
Base every insight on the live research data provided above.
When you cite a fact, it must come from the research, not generic knowledge.

REQUIRED JSON OUTPUT — follow exactly:

{
  "country_ranking": {
    "recommended_country": "string — single best country to enter first",
    "reasoning": "string — 3-4 sentences explaining why this country specifically,
                  referencing data from the live research",
    "entry_advantage": "string — the single biggest advantage of this country
                        for this specific company and product",
    "rankings": [
      {
        "country": "string",
        "overall_score": number (0-100),
        "scores": {
          "market_size": number (0-20),
          "competition_level": number (0-20, higher = less competition),
          "regulatory_ease": number (0-20, higher = easier compliance),
          "cultural_receptiveness": number (0-20),
          "infrastructure": number (0-20)
        },
        "one_line_verdict": "string — why this country ranks here",
        "best_for": "string — what type of company this country is ideal for",
        "main_challenge": "string — biggest obstacle specific to this country"
      }
    ]
  },

  "market_overview": {
    "target_country": "${isRecommendMode ? '[AI recommended country]' : target_country}",
    "target_continent": "${continent}",
    "market_size": "string — from live research",
    "growth_rate": "string — from live research",
    "key_trend": "string — most important market shift right now",
    "urgency_signal": "string — specific 2025-2026 regulation or event",
    "best_subsector": "string — most accessible subsector to enter first",
    "typical_sales_cycle": "string — realistic timeline",
    "average_deal_size": "string — estimated contract value range",
    "buyer_journey": "string — who initiates, evaluates, approves"
  },

  "company_summary": {
    "name": "string",
    "one_line_pitch": "string",
    "market_readiness_score": number (0-100),
    "market_readiness_label": "string",
    "critical_insight": "string",
    "biggest_blind_spot": "string",
    "unfair_advantage": "string"
  },

  "buyer_psychology": {
    "primary_fear": "string — what Western buyers fear about Chinese suppliers",
    "trust_builders": ["string", "string", "string"],
    "top_objections": ["string", "string", "string"],
    "winning_pitch_angle": "string — the framing that works best in this market"
  },

  "icp_matches": [
    {
      "rank": number,
      "company_name": "string — real company",
      "is_verified": false,
      "website": "string or null",
      "country": "string — must be in ${isRecommendMode ? continent : target_country}",
      "city": "string",
      "sector": "string",
      "company_size": "string",
      "why_they_match": "string — evidence-based, references live research",
      "what_they_are_replacing": "string — current solution they would switch from",
      "decision_maker_title": "string",
      "buying_trigger": "string — specific signal from live research if available",
      "buying_signal_status": "hot | warm | cool | none",
      "scores": {
        "product_fit": number (0-25),
        "market_readiness": number (0-25),
        "strategic_value": number (0-25),
        "accessibility": number (0-25),
        "overall": number (0-100)
      },
      "why_they_might_say_no": "string",
      "how_to_overcome_it": "string",
      "first_move": "string — exact action this week",
      "reference_value": "string — doors this customer opens"
    }
  ],

  "competitor_intelligence": [
    {
      "rank": number,
      "company_name": "string — real Western competitor from live research",
      "country": "string",
      "what_they_sell": "string",
      "who_they_target": "string",
      "positioning": "string",
      "pricing_signal": "string or null",
      "recent_news": "string — from live research if available",
      "weakness": "string — specific exploitable gap",
      "customer_complaints": "string",
      "how_to_beat_them": "string",
      "threat_level": "Low | Medium | High | Critical"
    }
  ],

  "distribution_channels": {
    "recommended_channel": "string",
    "reasoning": "string",
    "top_partners": [
      { "type": "string", "examples": "string", "why": "string" }
    ],
    "time_to_first_revenue": "string"
  },

  "regulatory_snapshot": {
    "certifications": [
      {
        "name": "string",
        "mandatory": boolean,
        "timeline": "string",
        "cost": "string",
        "body": "string — real certifying agency",
        "priority": "Immediate | Before Launch | Within 12 Months"
      }
    ],
    "biggest_risk": "string",
    "timeline_impact": "string"
  },

  "action_plan": {
    "market_entry_timeline": "string",
    "recommended_country": "string — reinforce the top country recommendation",
    "weeks": [
      {
        "period": "string",
        "theme": "string",
        "actions": ["string", "string", "string"],
        "milestone": "string",
        "cost_estimate": "string",
        "key_contact_type": "string"
      }
    ],
    "critical_path": "string",
    "first_action_tomorrow": "string"
  },

  "risk_assessment": [
    {
      "rank": number,
      "risk_title": "string",
      "risk_type": "string",
      "severity": "Low | Medium | High | Critical",
      "description": "string",
      "early_warning_sign": "string",
      "probability": "Low | Medium | High",
      "mitigation": "string",
      "cost_of_ignoring": "string",
      "timeline_to_impact": "string"
    }
  ]
}
`;

  try {
    const response = await fetch(
      'https://aiapi.orbitai.global/v1/chat/completions',
      {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Authorization': `Bearer ${process.env.ORBIT_API_KEY}`
        },
        body: JSON.stringify({
          model: 'gpt-5.4',
          messages: [
            { role: 'system', content: SYSTEM_PROMPT },
            { role: 'user', content: USER_PROMPT }
          ],
          temperature: 0.2,
          max_tokens: 4000
        })
      }
    );

    if (!response.ok) {
      return new Response(
        JSON.stringify({ error: `AI API error: ${response.status}` }),
        { status: 502, headers: { 'Content-Type': 'application/json' } }
      );
    }

    const aiData = await response.json();

    if (!aiData.choices?.[0]?.message?.content) {
      return new Response(JSON.stringify({ error: 'Empty AI response' }),
        { status: 502, headers: { 'Content-Type': 'application/json' } });
    }

    let content = aiData.choices[0].message.content.trim()
      .replace(/^```json\s*/i, '').replace(/^```\s*/i, '').replace(/```\s*$/i, '').trim();

    const report = JSON.parse(content);

    return new Response(JSON.stringify(report),
      { status: 200, headers: { 'Content-Type': 'application/json' } });

  } catch (e) {
    return new Response(JSON.stringify({ error: e.message }),
      { status: 500, headers: { 'Content-Type': 'application/json' } });
  }
}
```

---

## Step 4 — Country Ranking UI Component

Add this section at the top of your results, before ICP matches:

```jsx
{matches?.country_ranking && (
  <div className="card-animate c0" style={{
    background: '#1e293b',
    borderRadius: '12px',
    padding: '20px',
    marginBottom: '12px',
    borderLeft: '3px solid #f59e0b'
  }}>

    {/* Recommended country hero */}
    <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: '16px' }}>
      <div>
        <div style={{ fontSize: '9px', color: '#f59e0b', letterSpacing: '2px',
                      fontWeight: 500, marginBottom: '4px' }}>
          RECOMMENDED ENTRY POINT
        </div>
        <div style={{ fontSize: '22px', color: '#ffffff', fontWeight: 500 }}>
          🎯 {matches.country_ranking.recommended_country}
        </div>
        <div style={{ fontSize: '12px', color: '#94a3b8', marginTop: '6px',
                      lineHeight: 1.5, maxWidth: '400px' }}>
          {matches.country_ranking.reasoning}
        </div>
      </div>
      <div style={{ textAlign: 'right', minWidth: '120px' }}>
        <div style={{ fontSize: '9px', color: '#475569', marginBottom: '4px' }}>
          KEY ADVANTAGE
        </div>
        <div style={{ fontSize: '11px', color: '#60a5fa',
                      background: '#1e3a5f', padding: '8px',
                      borderRadius: '8px', lineHeight: 1.4 }}>
          {matches.country_ranking.entry_advantage}
        </div>
      </div>
    </div>

    {/* Country score table */}
    <div style={{ fontSize: '9px', color: '#475569', letterSpacing: '2px',
                  marginBottom: '10px' }}>
      ALL COUNTRIES RANKED
    </div>

    <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
      {matches.country_ranking.rankings
        ?.sort((a, b) => b.overall_score - a.overall_score)
        .map((country, i) => (
          <div key={i} style={{
            background: i === 0 ? '#1e3a5f' : '#0f172a',
            borderRadius: '8px',
            padding: '10px 14px',
            display: 'grid',
            gridTemplateColumns: '130px 1fr 60px',
            alignItems: 'center',
            gap: '12px',
            border: i === 0 ? '0.5px solid #2563eb' : '0.5px solid #1e293b'
          }}>
            <div>
              <div style={{ fontSize: '12px', color: '#ffffff', fontWeight: 500 }}>
                {i === 0 && '🏆 '}{country.country}
              </div>
              <div style={{ fontSize: '10px', color: '#475569', marginTop: '2px' }}>
                {country.main_challenge}
              </div>
            </div>
            <div>
              <div style={{ height: '3px', background: '#0f172a', borderRadius: '1px' }}>
                <div
                  className="bar"
                  style={{
                    '--target-width': `${country.overall_score}%`,
                    height: '3px',
                    background: i === 0 ? '#f59e0b' : '#334155',
                    borderRadius: '1px'
                  }}
                />
              </div>
              <div style={{ fontSize: '9px', color: '#475569', marginTop: '3px' }}>
                {country.one_line_verdict}
              </div>
            </div>
            <div style={{ textAlign: 'right', fontSize: '18px',
                          color: i === 0 ? '#f59e0b' : '#60a5fa', fontWeight: 500 }}>
              {country.overall_score}
            </div>
          </div>
        ))}
    </div>
  </div>
)}
```

---

## What Changes in the Demo

**Before:** User selects "Europe" → gets 5 European companies
**After:** User selects "Europe, Let AI Recommend" → AI scores Germany, France, Netherlands, Italy, UK, Sweden, Norway, Spain, Belgium, Poland → Recommends Germany → Shows ranked table → All matches are German companies → Explains exactly why Germany beats every other option for this specific product

On stage this looks like:

```
"I select Europe and let BridgeMatch recommend the best country.
Watch what happens..."

[AI runs 5 web searches in parallel — live data from today]

"Germany scores 87/100. Netherlands 79. France 71.
Here's exactly why Germany: the EU Battery Regulation
enforcement deadline is creating immediate supplier
urgency at German manufacturers like Sonnen and BMW.
No other European country has this level of active
procurement pressure right now."

[Judges see a ranked table of all 10 European countries
with scores and specific reasoning for each]

"Now all our partner matches are German companies,
with German decision makers, German procurement processes,
and German regulatory requirements."
```

